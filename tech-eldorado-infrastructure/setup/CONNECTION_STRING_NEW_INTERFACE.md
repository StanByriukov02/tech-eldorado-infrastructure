# CONNECTION STRING В НОВОМ ИНТЕРФЕЙСЕ SUPABASE

**ПРОБЛЕМА:** В Database Settings нет Connection string!

**РЕШЕНИЕ:** Connection string находится в Settings → API или собирается вручную!

═══════════════════════════════════════════════════════════════════════════════

## 🔍 ГДЕ НАЙТИ CONNECTION STRING

### ВАРИАНТ 1: Settings → API (РЕКОМЕНДУЕТСЯ!)

```
1. В левом меню: Settings (иконка шестерёнки ⚙️)
2. API (в списке настроек)
3. Найти секцию "Project API keys"
4. Прокрутить вниз до "Database" или "Connection string"
5. Там должен быть "Connection string" или "Database URL"
```

### ВАРИАНТ 2: Собрать вручную (САМЫЙ НАДЁЖНЫЙ!)

**Из того что ты видишь в Database Settings:**
- У тебя есть "Database password" (кнопка "Reset database password")
- Это значит пароль уже есть (который вводил при создании проекта)

**Что нужно:**
1. Settings → API → найти "Project URL"
2. Использовать Database password (который уже есть)
3. Собрать connection string вручную

═══════════════════════════════════════════════════════════════════════════════

## 🛠️ КАК СОБРАТЬ CONNECTION STRING ВРУЧНУЮ

### ШАГ 1: Получить Project URL

```
1. Settings → API (в левом меню)
2. Найти "Project URL": https://[project-id].supabase.co
3. Записать project-id (например: abcdefghijklmnop)
```

### ШАГ 2: Использовать Database Password

```
Это пароль который ты вводил при создании проекта (ШАГ 1.2)
Если забыл → можно сбросить в Database Settings → "Reset database password"
```

### ШАГ 3: Собрать Connection String

```
Формат:
postgresql://postgres:[PASSWORD]@db.[project-id].supabase.co:5432/postgres

Пример:
postgresql://postgres:MyPassword123@db.abcdefghijklmnop.supabase.co:5432/postgres

Где:
- postgres = пользователь (всегда postgres)
- [PASSWORD] = твой database password
- [project-id] = твой project ID из Project URL
- 5432 = порт (всегда 5432)
- postgres = имя базы данных (всегда postgres)
```

═══════════════════════════════════════════════════════════════════════════════

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### 1. Открыть Settings → API

```
В левом меню:
Settings ⚙️ → API
```

### 2. Найти Project URL

```
В секции "Project API keys" найти:
Project URL: https://[project-id].supabase.co

Скопировать project-id (часть между https:// и .supabase.co)
```

### 3. Использовать Database Password

```
Это пароль из ШАГ 1.2 (когда создавал проект)
Если забыл → Database Settings → "Reset database password"
```

### 4. Собрать Connection String

```
Заменить в этом шаблоне:
postgresql://postgres:[PASSWORD]@db.[project-id].supabase.co:5432/postgres

Например:
Project URL: https://abcdefghijklmnop.supabase.co
Password: MySecurePassword123

Connection String:
postgresql://postgres:MySecurePassword123@db.abcdefghijklmnop.supabase.co:5432/postgres
```

═══════════════════════════════════════════════════════════════════════════════

## ✅ ПРОВЕРКА

### Правильный формат:

```
postgresql://postgres:[password]@db.[project-id].supabase.co:5432/postgres
```

### Что должно быть:
- ✅ `postgresql://` в начале
- ✅ `postgres` как пользователь
- ✅ `[password]` - твой реальный пароль
- ✅ `db.[project-id].supabase.co` - host (с db. в начале!)
- ✅ `5432` - порт
- ✅ `postgres` - имя базы данных

═══════════════════════════════════════════════════════════════════════════════

## 🎯 БЫСТРЫЙ СПОСОБ

**Если хочешь быстро:**

1. Settings → API
2. Скопировать Project URL
3. Взять Database password (из ШАГ 1.2)
4. Собрать по формуле выше

**Это займёт 2 минуты!** ✅

═══════════════════════════════════════════════════════════════════════════════

**ГЛАВНОЕ:** Connection string НЕ в Database Settings, его нужно собрать вручную или найти в Settings → API!

