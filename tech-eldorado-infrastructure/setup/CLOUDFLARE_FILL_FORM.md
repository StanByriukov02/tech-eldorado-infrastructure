# КАК ЗАПОЛНИТЬ ФОРМУ CLOUDFLARE PAGES

**ТЫ НА СТРАНИЦЕ:** "Set up your application"

═══════════════════════════════════════════════════════════════════════════════

## ✅ ЧТО УЖЕ ЗАПОЛНЕНО (НЕ МЕНЯТЬ!)

```
✅ Repository/Project Path: StanByriukov02/tech-eldorado-infrastructure
✅ Project name: tech-eldorado
✅ Build command: npm run build
```

**ЭТО ПРАВИЛЬНО! НЕ МЕНЯТЬ!**

═══════════════════════════════════════════════════════════════════════════════

## ❌ ЧТО НЕ НУЖНО (ОСТАВИТЬ ПУСТЫМ ИЛИ УДАЛИТЬ)

### Deploy command:
```
❌ НЕ ЗАПОЛНЯТЬ!
❌ УДАЛИТЬ: npx wrangler deploy
❌ ОСТАВИТЬ ПУСТЫМ!
```

**Почему:** Это для Workers, не для Pages! Pages не нужен deploy command.

### Advanced settings:
```
❌ НЕ ОТКРЫВАТЬ!
❌ НЕ ЗАПОЛНЯТЬ:
   - Non-production branch deploy command
   - API token
   - API token name
```

**Почему:** Это для Workers, не для Pages!

═══════════════════════════════════════════════════════════════════════════════

## 📋 ЧТО НУЖНО ДОБАВИТЬ

### 1. Build output directory (ОБЯЗАТЕЛЬНО!)

**Где найти:**
- Прокрутить вниз на странице
- Найти поле "Build output directory" или "Output directory"
- Или в секции "Build settings"

**Что вписать:**
```
dist
```

### 2. Root directory (ОПЦИОНАЛЬНО)

**Где найти:**
- Рядом с "Build output directory"
- Или в секции "Build settings"

**Что вписать:**
```
frontend
```

**ИЛИ оставить пустым** если frontend в корне проекта.

### 3. Production branch

**Где найти:**
- Вверху страницы
- Или в секции "Build settings"

**Что выбрать:**
```
main
```

(Или `master` если у тебя master branch)

### 4. Environment Variables (ОБЯЗАТЕЛЬНО!)

**Где найти:**
- Прокрутить вниз
- Найти секцию "Environment variables" или "Variables"
- Или вкладку "Environment variables"

**Что добавить:**

```
Name: SUPABASE_URL
Value: https://kgregicsrvqrndublgmp.supabase.co

Name: SUPABASE_ANON_KEY
Value: [скопировать из .env файла]

Name: VITE_SUPABASE_URL
Value: https://kgregicsrvqrndublgmp.supabase.co

Name: VITE_SUPABASE_ANON_KEY
Value: [скопировать из .env файла]
```

**Где взять SUPABASE_ANON_KEY:**
1. Открыть файл `.env` в проекте
2. Найти строку: `SUPABASE_ANON_KEY=...`
3. Скопировать значение (БЕЗ `SUPABASE_ANON_KEY=`)

═══════════════════════════════════════════════════════════════════════════════

## 🎯 ИТОГОВАЯ ФОРМА

### Основные поля:

```
✅ Repository/Project Path: StanByriukov02/tech-eldorado-infrastructure
✅ Project name: tech-eldorado
✅ Production branch: main
✅ Build command: npm run build
✅ Build output directory: dist
✅ Root directory: frontend (или пусто)
❌ Deploy command: [ОСТАВИТЬ ПУСТЫМ!]
```

### Environment Variables:

```
✅ SUPABASE_URL = https://kgregicsrvqrndublgmp.supabase.co
✅ SUPABASE_ANON_KEY = [из .env]
✅ VITE_SUPABASE_URL = https://kgregicsrvqrndublgmp.supabase.co
✅ VITE_SUPABASE_ANON_KEY = [из .env]
```

═══════════════════════════════════════════════════════════════════════════════

## 🚀 ПОСЛЕ ЗАПОЛНЕНИЯ

```
1. Прокрутить вниз страницы
2. Найти кнопку "Save and Deploy" (синяя кнопка)
3. Нажать "Save and Deploy"
4. Дождаться deploy (~2-3 минуты)
```

═══════════════════════════════════════════════════════════════════════════════

## ⚠️ ВАЖНО

**Если не видишь поле "Build output directory":**
- Прокрутить вниз
- Или нажать "Show more" / "Advanced"
- Или искать в секции "Build settings"

**Если не видишь "Environment variables":**
- Прокрутить вниз
- Или искать вкладку "Environment variables" вверху
- Или добавить после создания проекта в Settings

**Если frontend ещё не создан:**
- Deploy упадёт с ошибкой - это нормально!
- Проект всё равно создан
- Можно настроить позже

═══════════════════════════════════════════════════════════════════════════════

**ГЛАВНОЕ:** 
- Удалить "Deploy command" (оставить пустым!)
- Добавить "Build output directory: dist"
- Добавить Environment Variables
- Нажать "Save and Deploy"

